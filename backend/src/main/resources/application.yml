server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/publicPay
    username: username
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create   # 开发阶段自动建表
    show-sql: true       # 控制台打印SQL